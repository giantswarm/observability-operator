appName: "observability-operator"
repoName: "observability-operator"
appCatalog: "control-plane-catalog"

# CAPI providers to test against when triggering `/run app-test-suites` from a PR
providers:
- capa
