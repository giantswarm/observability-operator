# This file was generated by observability-operator.
# It configures Alloy for OTLP metrics ingestion.
# - configMap is generated from events-logger.alloy.template and passed as a string
#   here and will be created by Alloy's chart.
# - Alloy runs as a deployment, with only 1 replica.
networkPolicy:
  cilium:
    ingress:
    - toPorts:
      - ports:
        # Alloy UI and API
        - port: "12345"
          protocol: TCP
        # OTLP gRPC receiver
        - port: "4317"
          protocol: "TCP"
        # OTLP HTTP receiver
        - port: "4318"
          protocol: "TCP"

alloy:
  alloy:
    configMap:
      create: true
      content: |-
        logging {
        	level  = "info"
        	format = "logfmt"
        }
        remote.kubernetes.secret "credentials" {
        	namespace = "kube-system"
        	name = "alloy-events"
        }
        // Basic authentication for Mimir
        otelcol.auth.basic "mimir_credentials" {
        	username = convert.nonsensitive(remote.kubernetes.secret.credentials.data["metrics-username"])
        	password = remote.kubernetes.secret.credentials.data["metrics-password"]
        }
        // OTLP receiver — accepts traces, metrics and logs from cluster workloads.
        otelcol.receiver.otlp "default" {
        	grpc {
        		endpoint = "0.0.0.0:4317"
        	}
        	http {
        		endpoint = "0.0.0.0:4318"
        	}
        	output {
        		metrics = [otelcol.processor.k8sattributes.default.input]
        	}
        }
        // Add Kubernetes metadata to all OTLP signals.
        otelcol.processor.k8sattributes "default" {
        	extract {
        		metadata = [
        			"k8s.namespace.name",
        			"k8s.pod.name",
        			"k8s.container.name",
        		]
        		label {
        			key = "observability.giantswarm.io/tenant"
        			tag_name = "giantswarm.tenant"
        		}
        		otel_annotations = true
        	}
        	output {
        		metrics = [otelcol.processor.transform.default.input]
        	}
        }
        // Add cluster metadata to traces, metrics and logs
        otelcol.processor.transform "default" {
        	error_mode = "ignore"
        	metric_statements {
        		context = "resource"
        		statements = [
        			`set(attributes["giantswarm.cluster.id"], "test-cluster")`,
        			`set(attributes["giantswarm.cluster.type"], "workload_cluster")`,
        			`set(attributes["giantswarm.cluster.organization"], "test-organization")`,
        			`set(attributes["giantswarm.cluster.provider"], "capa")`,
        		]
        	}
        	output {
        		metrics = [otelcol.processor.batch.default.input]
        	}
        }
        // Batch processor for efficient export
        otelcol.processor.batch "default" {
        	send_batch_size     = 8192
        	send_batch_max_size = 0
        	timeout             = "200ms"
        	output {
        		metrics = [
        			otelcol.processor.filter.giantswarm.input,
        		]
        	}
        }
        // Per-tenant routing
        otelcol.processor.filter "giantswarm" {
        	metrics {
        		datapoint = [`resource.attributes["giantswarm.tenant"] != "giantswarm"`]
        	}
        	output {
        		metrics = [otelcol.exporter.otlphttp.giantswarm_mimir.input]
        	}
        }
        // OTLP HTTP exporter for metrics — tenant giantswarm → Mimir
        otelcol.exporter.otlphttp "giantswarm_mimir" {
        	client {
        		auth     = otelcol.auth.basic.mimir_credentials.handler
        		endpoint = convert.nonsensitive(remote.kubernetes.secret.credentials.data["metrics-otlp-write-api-url"])
        		tls {
        			insecure_skip_verify = false
        		}
        		headers = {
        			"X-Scope-OrgID" = "giantswarm",
        		}
        	}
        }
    # Resource configuration based on https://github.com/giantswarm/giantswarm/issues/32655
    # CPU limits updated in https://github.com/giantswarm/giantswarm/issues/34619 to prevent throttling
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
        - ALL
      readOnlyRootFilesystem: false
      runAsUser: 10
      runAsGroup: 10
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault
  controller:
    type: deployment
    replicas: 1
  crds:
    create: false
  extraObjects:
<<<<<<< HEAD
  # OTLP gateway service for traces, metrics and logs
=======
  # OTLP gateway service for traces/metrics/logs
>>>>>>> 314d97a (style: apply gofmt formatting)
  - apiVersion: v1
    kind: Service
    metadata:
      annotations:
        meta.helm.sh/release-name: alloy-events
        meta.helm.sh/release-namespace: kube-system
      labels:
        app.kubernetes.io/component: networking
        app.kubernetes.io/instance: alloy-events
        app.kubernetes.io/managed-by: Helm
        app.kubernetes.io/name: alloy
        app.kubernetes.io/part-of: alloy
        application.giantswarm.io/team: atlas
        giantswarm.io/managed-by: alloy-events
        giantswarm.io/service-type: managed
        helm.sh/chart: alloy-1.1.0
      name: otlp-gateway
      namespace: kube-system
    spec:
      ports:
      - appProtocol: grpc
        name: otlp
        port: 4317
        protocol: TCP
        targetPort: 4317
      - appProtocol: http
        name: otlp-http
        port: 4318
        protocol: TCP
        targetPort: 4318
      selector:
        app.kubernetes.io/instance: alloy-events
        app.kubernetes.io/name: alloy
      type: ClusterIP

verticalPodAutoscaler:
  enabled: true
  # VPA configuration based on https://github.com/giantswarm/giantswarm/issues/32655
  resourcePolicy:
    containerPolicies:
    - containerName: alloy
      controlledResources:
      - memory
      controlledValues: "RequestsAndLimits"
